import { PageComponent, PageElement } from './src/component';
import { child, element, text, byDataBind, byPlaceholder, byText } from './src/decorators';

export { PageComponent, PageElement, child, element, text, byDataBind, byPlaceholder, byText }
